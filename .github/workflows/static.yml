# This workflow is superseded by deploy.yml which builds the Vite app before deploying.
# Kept here to avoid CI errors on open PRs; does nothing.
name: (Deprecated) Deploy static content to Pages

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy.yml instead"
